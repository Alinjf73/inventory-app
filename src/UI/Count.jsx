function Count({ productCount }) {
  return (
    <div className="border-2 border-primary-900 bg-secondary-200 text-primary-900 rounded-full size-8 flex items-center justify-center">
      {productCount}
    </div>
  );
}

export default Count;
